//
// Created by Nikolay Kanchevski on 25.04.24.
//

#pragma once

namespace SierraEngine
{

    class TestRenderer
    {
    public:
        /* --- CONSTRUCTORS --- */
        TestRenderer() = default;

        /* --- POLLING METHODS --- */

        /* --- SETTER METHODS --- */

        /* --- GETTER METHODS --- */

        /* --- OPERATORS --- */
        TestRenderer(const TestRenderer &) = delete;

        TestRenderer &operator=(const TestRenderer &) = delete;

        /* --- DESTRUCTOR --- */
        ~TestRenderer() = default;

    private:

    };

} // SierraEngine
